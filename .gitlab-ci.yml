image: maven:3-jdk-11

cache:
  paths:
    - .m2/repository/

stages:
  - build
  - test

build:
  stage: build
  script:
    - cd Backend
    - mvn compile

test:
  stage: test
  script:
    - cd Backend
    - mvn test